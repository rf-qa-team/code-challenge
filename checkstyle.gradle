checkstyle {
    toolVersion = '8.42'
    ignoreFailures = false
    showViolations = true
    configFile = file("./checkstyle.xml")
}

checkstyleMain {
    source = 'src/main/java'
}

checkstyleTest {
    source = 'src/test/java'
}